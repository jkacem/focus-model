"""
Provider package — abstract interfaces + concrete implementations.

Current providers:
  local       → Piper TTS + Whisper STT + HTTP backend calls
  gemini_live → Google Gemini Live (stub, future migration)
"""
